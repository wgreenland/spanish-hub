---
import Layout from "../components/Layout.astro";
import Auth from "../components/Auth.jsx";

let user = null;

function handleLogin(u) {
  user = u;
}
---

<Layout>
  <h1>🔑 Login</h1>
  <Auth client:load onLogin={handleLogin} />
</Layout>
